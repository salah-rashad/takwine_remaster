
import 'package:flutter/material.dart';

class MyCertificatesTabView extends StatelessWidget {
  const MyCertificatesTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(runtimeType.toString()),
    );
  }
}